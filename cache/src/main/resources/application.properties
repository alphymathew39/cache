# Enable caching
spring.cache.type=SIMPLE

# Cache names
spring.cache.cache-names=sumCache

# Cache configuration
spring.cache.caffeine.spec=maximumSize=100,expireAfterWrite=10s

management.endpoints.web.exposure.include=*